import React from "react";
import styled from "styled-components";

const Wrapper = styled.div``;

const StaffSchedule = () => {
  return <Wrapper>staff schedule</Wrapper>;
};

export default StaffSchedule;
